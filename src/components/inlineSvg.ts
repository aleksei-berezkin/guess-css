import makeStyles from '@material-ui/core/styles/makeStyles';

export const useInlineSvg = makeStyles({
    small: {
        marginBottom: '-.15em'
    },
    score: {
        marginBottom: '-.18em'
    },
});
